PROMPT CLAUDE CODE — PORTRAIT COMPLET VISIBLE
=============================================

UNE SEULE MODIFICATION. NE RIEN TOUCHER D'AUTRE.

Trouver dans InteractiveFantasyCard.css (ou InteractiveCard.css)
le sélecteur .layer-character img

Remplacer TOUTES ses propriétés par exactement ceci :

.layer-character {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.layer-character img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center center;
  filter: drop-shadow(0 0 20px rgba(200, 100, 0, 0.4));
}

C'EST TOUT.

object-fit: contain garantit que l'image entière est visible,
rien n'est coupé, le ratio est respecté.

Ensuite :
npm run build
git add .
git commit -m "fix: portrait contain - image entière visible"
git push origin main
